global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'crypto-wallet-app'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets: ['app:8080'] # 'app' matches the service name in docker-compose